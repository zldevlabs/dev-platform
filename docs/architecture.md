# Architecture
Frontend: Flutter (Material 3)
State: Provider (simple, replaceable)
Backend: None for v1 (local). Future: Firebase for auth + storage.
AI: Codex (dev agent) used during development loop; later move to server-side orchestrator.
